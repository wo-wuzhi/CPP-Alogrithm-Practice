#include<bits/stdc++.h>
using namespace std;

int main(){
    int n,g,f;
    cin>>n>>g>>f;
    vector<int>p(n,0);
    vector<int>r(n,0);
    for(int i=0;i<n;i++){
        cin>>p[i]>>r[i];
    }
    未完成
    return 0;
}